using TypeListExplanation;

var students = IEnumerableSample.GetStudents();

foreach (var student in students)
{
    Console.WriteLine(student);
}

//var total = students.Count();
//Console.WriteLine(total);
